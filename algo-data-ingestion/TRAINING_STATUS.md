# Model Training Status (XGB · TCN · Blender)

_Last updated: 2025-11-17 14:38 UTC_

> Update 2025-11-17: Logged the new time-series CV + random-search sweeps (shared search/cv configs, `experiments/hparam_search/*` outputs) and the promoted configs in `configs/best_model_configs.{yaml,json}`, plus the deployability-aware early stopping for sequence models. The symbol-aware relaxed gate + parity stack from 2025-11-13 remains unchanged.

## Quick Status
- **Base XGB (Calmon relaxed gate)** – `models/base_xgb_h120_calmon_spread0` retains `final_equity 4.48`, Sharpe 108, 3.6 k toggles under the relaxed mask, and the deployable manifest now keeps only `prob ≥ 0.2`, `min_hold 10`, `long_only`, delegating spread/rvol guardrails to the trading layer. The refreshed Oct 1 – Oct 28 2025 replay (`models/oos_replay_summary_latest.json`) records 12 gate hits, 8 toggles, `final_equity 1.2336`, and `gate_coverage 2.99e-4`, restoring a measurable live floor. Retrains over `datasets/market_multi_3symbol_1m.parquet` now auto-load `release/symbol_gates/market_multi_3symbol_1m.json`, so BTC/ETH/SOL share the sanitizer-derived `hl_spread`, `rvol`, and liquidity caps downstream.
- **TCN suite (Calmon relaxed)** – horizons 60/120/180 still clear 5 bps costs (training `final_equity` 1.28/3.62/1.85 with ≤200 toggles); the deployable manifest is now reduced to `prob ≥ 0.25`, `min_hold 10`, `long_only`, with spread/vol caps handled downstream. Replay coverage remains (`gate_hits 4/31/2`, `toggle_count 4/62/2`, `gate_coverage 4.73e-4/7.71e-4/4.23e-4`, `final_equity 1.03/1.94/1.01` on Oct 2025). `training/infer.predict_tcn` continues to batch by stride so stride-1 experiments stay memory-safe.
- **Logistic blender (elastic-net)** – `models/blender_h120_v6` now posts `final_equity 4.48`, Sharpe 206.8, 4 809 toggles at threshold 0.5; the deployable manifest (`prob ≥ 0.5`, `rvol_20 ≤ 5e-4`, `min_hold 10`) sustains ≈15.8 % gate coverage (6 346 toggles) on Oct 2025. Sandbox stride‑1 prototypes (`models/blender_h120_stride1_v2`) illustrate how collapsing the smoothing window to one bar drops gate coverage to ≈0.2 % with 134 toggles while preserving equity, giving us an upper bound on turnover.
- **Hyperparameter sweeps (time-series CV)** – Random-search runs now live under `experiments/hparam_search/{xgb,tcn,transformer}` using expanding splits from `configs/cv_config.yaml` (15D validation, 1D gap). Shared search spaces sit in `configs/hparam_spaces.yaml`; promoted configs are committed to `configs/best_model_configs.{yaml,json}` (`xgb_trial_010`, `tcn_trial_011`, `transformer_trial_023`). Sequence models early-stop on Sharpe with `cost_bps`/`min_hold_bars` applied when validation returns are provided.
- **CI guardrails & monitoring** – `.github/workflows/ci.yml` runs ingestion, regression, and training suites with manifest tests split out and now adds a forward replay guardrail for `tcn_h{60,120,180}_calmon_relaxed` (fails when deployable coverage <5e-4 or `final_equity < 1.2`); `app/monitoring/model_metrics.py` surfaces gate coverage, RSS share, and probability σ gauges so Prometheus alerts trigger when deployable behaviour drifts; the scheduler’s inference jobs and trading dry-run service are covered by `tests/ingestion_service/test_manifest_integration.py`, `tests/ingestion_service/test_scoring_service.py`, and `tests/trading/test_service.py`, keeping queue → audit coverage under CI.
- **Feature parity workflow** – `scripts/export_feature_slice.py` now exports Redis-feature slices that mimic scheduler ingestion, `scripts/compare_feature_stats.py` diffs them against the training parquet (`hl_spread`, `hl_spread_z`, `rvol_20`, `base_prob`), and payloads land in `release/calibration/latest` for gating sign-off before widening manifests.

## Data Landscape
- **Year-wide minute feed** – `datasets/market_btcusdt_1m_2024_2025.parquet`, 959 039 bars from 2024-01-01 ➜ 2025-10-27 23:58 UTC. Spread stats still align with the relaxed training gate (avg `hl_spread` ≈6.8 bps, q99 ≈34 bps).
- **Multi-symbol relaxed feed** – `datasets/market_multi_3symbol_1m.parquet` (BTC/ETH/SOL, ≈1.19 M rows through 2025-11-04) is produced by the sanitizer pipeline (`training/data.sanitize_market_dataset`) so per-symbol log-return caps, liquidity ranks, and deduped timestamps stay consistent when comparing live vs training gates.
- **Blender matrix (RSS enriched)** – `datasets/blender_matrix_2024-09_to_2025-09_rss_latest.parquet` (mirrored as `..._2025-10_rss_latest.parquet`), 606 121 rows covering 2024-09-01 ➜ 2025-10-26; the refreshed stats file now records window start/end and probability means (`base_prob_mean 0.515`, `tcn_prob_mean 0.509`).
- **Forward replay matrix** – `datasets/blender_matrix_2025-10_to_2025-11_with_preds.parquet`, 40 201 rows (2025-10-01 ➜ 2025-10-28 22:00) with base/TCN/blender probabilities embedded for OOS diagnostics.
- **Gate coverage replay** – `models/base_xgb_h120_calmon_spread0/live_gate_coverage.csv` now spans 0.00896 %–0.02985 % with the Oct 2025 append (`fires 12`, `coverage 2.99e-4`, ratio_vs_baseline 2.71×); `models/oos_replay_summary_latest.json` mirrors the same floor and `models/tcn_gate_replay_summary.json` captures the new TCN coverage band (h60 4.73e-4, h120 7.71e-4, h180 4.23e-4).

## Horizon-120 XGBoost
- CLI defaults in `scripts/train_base_gbdt.py` now align with the calmon relaxed profile (`--max-spread-z 0.25`, `--max-rvol20 2e-4`, no probability filter) while persisting the deployable gate inside the manifest. Diagnostic output captures calendar-month metrics, RSS audits, and threshold grids.
- Spread stress tests (`spread_scale` ∈ {0, 0.05, 0.1, 0.2}) retain 4.48 equity with identical turnover, showing robustness to 20 % cost inflation.
- `training/reporting.ensure_kpi_schema` normalises KPI payloads, and `scripts/report_shortlist.py` curates `models/report_shortlist.json` so reviewers can spot deployable variants without manual diffing.
- `tests/regression/test_manifest_gating.py` now runs in CI to guarantee each manifest’s gate config and threshold stay in sync with `report.json`; `tests/regression/test_report_shortlist.py` ensures the shortlist CLI continues to surface the Calmon baseline.
- Oct 1 – Oct 28 2025 replay (`models/oos_replay_summary_latest.json`) retains 4.48 equity under the relaxed gate and now reports 12 deployable gate hits (8 toggles, `final_equity 1.2336`) after widening the manifest—monitor coverage closely while iterating on additional guardrails.

## TCN Suite
- `scripts/train_tcn.py` refresh adds fold logit persistence, stride control, and the relaxed gate defaults. Each manifest references `fold_logits.parquet`, the monthly probability σ table, and the shared inference gate.
- **Key metrics** (`cost_bps=5`, relaxed training gate):
  - `models/tcn_h60_calmon_relaxed`: final_equity **1.277**, Sharpe 38.1, total_turnover 82, selected_threshold 0.55.
  - `models/tcn_h120_calmon_relaxed`: final_equity **3.624**, Sharpe 99.8, total_turnover 128, selected_threshold 0.55.
  - `models/tcn_h180_calmon_relaxed`: final_equity **1.852**, Sharpe 68.8, total_turnover 36, selected_threshold 0.55.
- `models/oos_replay_summary_latest.json` and `models/tcn_gate_replay_summary.json` document training-vs-inference gate behaviour for audit trails. The Oct 2025 forward replay mirrors the base finding in reverse: relaxed gates stay profitable and now produce deployable coverage (h120 `gate_hits 31`, `final_equity 1.94`), so focus shifts to keeping coverage above the 5e-4 guardrail while validating h60/h180 behaviour.

## Blender & Meta Label
- `scripts/build_blender_matrix.py` now builds the RSS-enriched matrix with intraday spike features, exponential decays, probability momentum, and ensures label continuity. Outputs include a JSON summary and leverage `settings.FSSPEC_STORAGE_OPTIONS` for remote stores; the forward replay variant writes `datasets/blender_matrix_2025-10_to_2025-11_with_preds.parquet`.
- `scripts/train_blender.py` consumes the matrix, standardises features, and runs an elastic-net sweep (`l1_ratio` grid). `models/blender_h120_v6` demonstrates that once RSS spikes are engineered into continuous signals the logistic stack can operate with 4 809 toggles at threshold 0.5 while keeping the 5 bps cost budget, and the relaxed training run now records Sharpe 206.8.
- New sandboxed runs (`models/blender_h120_stride1_v2`) explore stride‑1 gating to quantify the turnover trade-off when smoothing is removed; gate share drops to ≈0.2 % (134 toggles) under the deployable manifest, providing an upper-bound on turnover when smoothing is relaxed.
- Forward diagnostics combine the Oct 2025 matrix with `models/oos_replay_summary_latest.json`; the deployable manifest now fires 6 346 toggles (`gate_coverage ≈ 15.8 %`), validating the eased thresholds ahead of broader monitoring rollout, while `scripts/run_oos_eval.py --family blender` keeps base/TCN/blender audits aligned.
- `scripts/train_meta_label.py` benefits from the shared relaxed gate yet still lacks a stable decision surface—the current meta artifacts remain placeholders until the blender/base probabilities regain dynamic range on forward months.

## Operational Follow-Ups
1. Track the refreshed TCN deployable gate across horizons and ensure `model_gate_coverage_ratio` (base + TCN + blender) stays above the new Oct 2025 floor; document any further threshold tweaks alongside replay metrics.
2. Thread `training/infer.py`’s `load_manifest_artifacts`/`score_base_with_manifest` helpers through the production API, ensuring Prometheus gauges expose gate coverage, RSS share, and probability σ for every inference batch and that stride-aware batching is exercised end-to-end.
3. Monitor RSS coverage via the new gauges + alerts, keep the no-RSS fallback path ready when `model_rss_minute_spike_share` drops below the manifest threshold, and document when lowering the smoothing stride is acceptable to boost coverage.
4. Revisit meta-label training only after deployable coverage stabilises across base/TCN/blender (including the stride experiments); until then prioritise hardening the ensemble and regression checks.
5. Exercise the scheduler `INFER_JOBS` + trading dry-run loop during staging: confirm Redis queue depth stays bounded, audit streams capture gate + trade events, and Grafana’s trading overview dashboard aligns with the Prometheus counters (`trading_trade_attempts_total`, `trading_position_active`).
